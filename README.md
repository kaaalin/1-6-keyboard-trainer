# 1–6 Keyboard Trainer

Upload these files to a GitHub repository, then import the repository into Vercel.

## Local run

```bash
npm install
npm run dev
```

## Vercel build settings

Framework preset: Vite  
Build command: npm run build  
Output directory: dist
